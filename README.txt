Attaching a .jar file which is my HTTP Client developed in Java, a shell script called client which accepts 2 parameters: method and URL, and a packages file which mentions required packages (open jdk 7). I have also attahed source.zip which contains source code for my HTTP Client. To run the HTTP Client, run the client shell script as ./client along with 2 parameters: <Method> <URL>.
